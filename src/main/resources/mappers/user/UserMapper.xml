<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.homesage.domain.user.mapper.UserMapper">

    <select id="findAllUsers" resultType="com.ssafy.homesage.domain.user.model.entity.User">
        SELECT email, password, name, role FROM users
    </select>

    <select id="checkDuplicateEmail" resultType="boolean" parameterType="string">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

    <insert id="signUp" parameterType="com.ssafy.homesage.domain.user.model.entity.User">
        INSERT INTO users (email, password, name, role)
        VALUES (#{email}, #{password}, #{name}, #{role})
    </insert>

    <select id="login" resultType="com.ssafy.homesage.domain.user.model.entity.User" parameterType="string">
        SELECT email, password, name, role FROM users WHERE email = #{email}
    </select>

</mapper>
